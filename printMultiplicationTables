function printMultiplicationTables(x) {
    for (i = 1; i <= x; i++) {
        for (a = 1; a <= 10; a++) {
            console.log(i + ' x ' + a + ' = '+ i * a);
        }
    }
}

printMultiplicationTables(10);