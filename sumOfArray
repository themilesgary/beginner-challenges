let a = [1,-1,2,-2,3,-3];

let c = [1.0,1.1,1.2,1.3,1.4];

function sumArray(array) {
    let sum = 0;
    for (i=0;i<array.length;i++) {
        sum = sum + array[i];
    }
    return sum;
}

console.log(sumArray(c));